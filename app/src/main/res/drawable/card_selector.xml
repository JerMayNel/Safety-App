<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:drawable="@drawable/card_highlight" android:state_selected="true" />
    <!-- Default state -->
    <item android:drawable="@android:color/transparent" />
</selector>
